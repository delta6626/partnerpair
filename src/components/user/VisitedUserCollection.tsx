import type { ViewerMetaData } from "../../../shared/types/ViewerMetaData";

export const VisitedUserCollection = ({ visitedUsers }: { visitedUsers: ViewerMetaData[] }) => {
  return <div className=""></div>;
};
