export const PricingCard = ({
  tierName,
  tierSubtitle,
  tierPrice,
  tierFeatures,
  tierLink,
  isRecommended,
}: {
  tierName: string;
  tierSubtitle: string;
  tierPrice: string;
  tierFeatures: string[];
  tierLink: string;
  isRecommended: boolean;
}) => {
  return <div className=""></div>;
};
