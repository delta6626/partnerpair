export const PricingCard = ({
  tierName,
  tierPrice,
  tierFeatures,
  tierLink,
  isRecommended,
}: {
  tierName: string;
  tierPrice: string;
  tierFeatures: string[];
  tierLink: string;
  isRecommended: boolean;
}) => {
  return <div className=""></div>;
};
